// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: strategy.proto

package genconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	gen "github.com/bufbuild/buf-examples/gen"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// StrategyServiceName is the fully-qualified name of the StrategyService service.
	StrategyServiceName = "strategy.StrategyService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// StrategyServiceRegisterStrategyProcedure is the fully-qualified name of the StrategyService's
	// RegisterStrategy RPC.
	StrategyServiceRegisterStrategyProcedure = "/strategy.StrategyService/RegisterStrategy"
)

// StrategyServiceClient is a client for the strategy.StrategyService service.
type StrategyServiceClient interface {
	RegisterStrategy(context.Context, *connect.Request[gen.RegisterStrategyRequest]) (*connect.Response[gen.RegisterStrategyResponse], error)
}

// NewStrategyServiceClient constructs a client for the strategy.StrategyService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewStrategyServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) StrategyServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	strategyServiceMethods := gen.File_strategy_proto.Services().ByName("StrategyService").Methods()
	return &strategyServiceClient{
		registerStrategy: connect.NewClient[gen.RegisterStrategyRequest, gen.RegisterStrategyResponse](
			httpClient,
			baseURL+StrategyServiceRegisterStrategyProcedure,
			connect.WithSchema(strategyServiceMethods.ByName("RegisterStrategy")),
			connect.WithClientOptions(opts...),
		),
	}
}

// strategyServiceClient implements StrategyServiceClient.
type strategyServiceClient struct {
	registerStrategy *connect.Client[gen.RegisterStrategyRequest, gen.RegisterStrategyResponse]
}

// RegisterStrategy calls strategy.StrategyService.RegisterStrategy.
func (c *strategyServiceClient) RegisterStrategy(ctx context.Context, req *connect.Request[gen.RegisterStrategyRequest]) (*connect.Response[gen.RegisterStrategyResponse], error) {
	return c.registerStrategy.CallUnary(ctx, req)
}

// StrategyServiceHandler is an implementation of the strategy.StrategyService service.
type StrategyServiceHandler interface {
	RegisterStrategy(context.Context, *connect.Request[gen.RegisterStrategyRequest]) (*connect.Response[gen.RegisterStrategyResponse], error)
}

// NewStrategyServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewStrategyServiceHandler(svc StrategyServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	strategyServiceMethods := gen.File_strategy_proto.Services().ByName("StrategyService").Methods()
	strategyServiceRegisterStrategyHandler := connect.NewUnaryHandler(
		StrategyServiceRegisterStrategyProcedure,
		svc.RegisterStrategy,
		connect.WithSchema(strategyServiceMethods.ByName("RegisterStrategy")),
		connect.WithHandlerOptions(opts...),
	)
	return "/strategy.StrategyService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case StrategyServiceRegisterStrategyProcedure:
			strategyServiceRegisterStrategyHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedStrategyServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedStrategyServiceHandler struct{}

func (UnimplementedStrategyServiceHandler) RegisterStrategy(context.Context, *connect.Request[gen.RegisterStrategyRequest]) (*connect.Response[gen.RegisterStrategyResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("strategy.StrategyService.RegisterStrategy is not implemented"))
}
